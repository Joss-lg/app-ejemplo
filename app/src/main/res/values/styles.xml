<!-- res/values/styles.xml -->
<resources>

    <style name="CustomCalendarView" parent="android:Widget.CalendarView">
        <item name="android:itemBackground">@drawable/calendar_selector</item>
        <item name="android:dateTextAppearance">@style/CustomDateAppearance</item>
    </style>

    <style name="CustomDateAppearance">
        <item name="android:textColor">#000000</item> <!-- Color del texto de las fechas -->
    </style>

</resources>
